global:
  owner: LarsWallimann
  qsname: clxs-cloudformation-pipeline
  regions:
    - eu-west-1
    - eu-west-2
project:
  name: clxs-cfn-project
tests:
  eu-regions:
    template_file: ./templates/workload.template
    parameter_input: input.json
